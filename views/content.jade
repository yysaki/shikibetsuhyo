doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title #{game.title} - 不思議のダンジョン 値段識別表
    meta(name="viewport" content="width=device-width, initialize-scale=1")
    link(rel="stylesheet", href="/css/bundle.css")
    script(src="/js/bundle.js", charset="UTF-8")

  body
    - each list in game.lists
      div(data-role="page" id=list.type)
        div(data-role="header" data-position="fixed")
          div(data-role="navbar")
            ul
              - each navbarList in game.lists
                li(class="six-tab")
                  - var activeClass = (list.type == navbarList.type) ? ["ui-btn-active", "ui-state-persist"] : ""
                  a(href="./##{navbarList.type}" class=activeClass)= navbarList.name
        div(data-role="content")
          ul(data-role="listview")
            - each item, index0 in list.items
              if list.isHeadOfGroup(item)
                li(data-role="list-divider") 買: #{item.buy} / 売: #{item.sell}
              li(class="item")
                label
                  input(type="checkbox" data-id=(index0 + 1) data-type=list.type)
                  span= item.name
        div(data-role="footer" data-position="fixed")
          - var footerButton = ["ui-btn", "ui-corner-all", "ui-btn-inline", "ui-mini", "ui-btn-icon-left"]
          a(href="" class=footerButton class="reset-checkboxes footer-button-left ui-icon-alert") Clear
          h1= game.shortTitle
          a(href="./#about" class=footerButton class="footer-button-right ui-icon-info" data-transition="pop") About

    div(data-role="page" data-dialog="true" id="about")
      | !{about}
